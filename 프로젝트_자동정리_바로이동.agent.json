{
  "name": "프로젝트_자동정리_바로이동",
  "description": "C:\\HVDC PJT 와 C:\\cursor-mcp 를 프로젝트 단위로 묶어 표준 스키마로 즉시 이동",
  "steps": [
    {"run": "devmind scan --paths \"C:\\HVDC PJT\" \"C:\\cursor-mcp\" --max-size 500000000 --sample-bytes 4096"},
    {"run": "devmind rules --config rules.yml --emit .cache/scores.json"},
    {"run": "devmind cluster --model gpt-4o --budget $4 --project-mode true --out .cache/projects.json"},
    {"run": "devmind organize --projects .cache/projects.json --target \"C:\\PROJECTS_STRUCT\" --schema schema.yml --clusters .cache/projects.json --mode move --conflict version"},
    {"run": "devmind report --clusters .cache/projects.json --journal .cache/journal.jsonl --out reports/projects_summary.html"}
  ],
  "env": {"OPENAI_API_KEY": "${env:OPENAI_API_KEY}"},
  "workingDirectory": "${workspaceRoot}",
  "timeout": 0
}
